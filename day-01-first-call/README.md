# Day 01: First LLM API Call

Run the first Poe OpenAI-compatible LLM API call.

首次运行需先在项目根目录安装依赖（见根目录 `README.md`）。

在项目根目录执行（需已设置环境变量 `POE_API_KEY`）：

```bash
source .venv/bin/activate
python3 day-01-first-call/main.py
```

The API key is read from `POE_API_KEY`.

笔记：[docs/notes/week-01/day-01-first-call.md](../docs/notes/week-01/day-01-first-call.md)
